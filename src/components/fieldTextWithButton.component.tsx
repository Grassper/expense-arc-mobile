import styled from 'styled-components/native';

const FieldTextWithButton = styled.View`
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
`;

export default FieldTextWithButton;
